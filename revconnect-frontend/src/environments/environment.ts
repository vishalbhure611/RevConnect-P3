export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};


//The environment file stores application configuration like API base URL and 
// production flag for different environments (dev/prod).